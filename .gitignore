node_modules/
.next/
out/
build/
dist/
.env
.env.local
.env*.local
# Biến môi trường cho `wrangler dev` — cùng vai trò với .env.local.
.dev.vars
.dev.vars.*
*.log
npm-debug.log*
.DS_Store
.vercel
# Kết quả của `netlify build` — khoảng 119 MB mã sinh tự động.
.netlify/
# Kết quả của `opennextjs-cloudflare build` và bộ nhớ tạm của Wrangler.
.open-next/
.wrangler/
netlify.toml.bak
*.tsbuildinfo
next-env.d.ts
.supabase/
coverage/
supabase/.temp/
supabase/.branches/

# Thông tin kết nối cơ sở dữ liệu để sao lưu tại máy — KHÔNG BAO GIỜ commit.
scripts/backup/.env.backup
backups/

# Thông tin định danh cá nhân của Bên A (số CCCD) — KHÔNG BAO GIỜ commit.
# Dùng .ben-a.example.json làm mẫu.
supabase/data-imports/scripts/.ben-a.json
